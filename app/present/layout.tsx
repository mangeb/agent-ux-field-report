import "../globals.css";
import "./present.css";

export const metadata = {
  title: "Field Report · Present",
  description: "Slide deck — how 24 agent products look today, and what to take to Chrome.",
};

export default function PresentLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
